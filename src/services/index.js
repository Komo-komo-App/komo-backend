const authorService = require("./authorService");
const bookService = require("./bookService");
const chapterService = require("./chapterService");
const readerSevice = require("./readerService");

module.exports = {
  authorService,
  bookService,
  chapterService,
  readerSevice,
};
